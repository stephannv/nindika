.dropdown.with-arrow
  button#games-sort-button.btn(data-toggle="dropdown" type="button" aria-haspopup="true" aria-expanded="false")
    i.fa.fa-sort-amount-down-alt
    |  Ordenar

  .dropdown-menu.dropdown-menu-right(aria-labelledby="games-sort-button")
    - options.each do |option|
      = link_to sort_path(option), class: dropdown_item_class(option) do
        == I18n.t("games.sort_options.#{option}")
